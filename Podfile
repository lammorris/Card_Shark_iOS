# Uncomment the next line to define a global platform for your project
platform :ios, '11.0'

# Comment the next line if you don't want to use dynamic frameworks
use_frameworks!

target 'CardDeck' do
  pod 'SwiftLint'
  
  pod 'SDWebImage', '~> 5.0'

  target 'CardDeckTests' do
    inherit! :search_paths
    # Pods for testing
  end
end
